/**
 * config.js
 * ------------------------------------------------------------------
 * 対象回号は自動取得しない（仕様書 §11: 結果自動取得は今回不要／本番自動取得は未実装のまま）。
 * Phase 1ではこの値をテスト用の固定値とし、画面にそのまま表示する。
 * 実際の回号が確定次第、この1箇所の数値だけを書き換えて運用する。
 * ------------------------------------------------------------------
 */
window.LotoConfig = {
  CURRENT_DRAW_NUMBER: 1850, // Phase 1テスト用の固定値。運用時はここだけ更新する。
};
